<h1>商品詳細</h1>
<% if @item.image.attached? %>
<%= image_tag @item.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no_image', size: "200x200" %>
<% end %>

<h2>商品名</h2>
<%= @item.name %>

<h2>商品説明</h2>
<%= @item.introduction %>

<h2>税込価格(税抜価格)</h2>
<%= (@item.price * 1.1).floor %>(<%= @item.price %>)円

<p><%= link_to "編集する", edit_admin_item_path(@item.id) %></p>