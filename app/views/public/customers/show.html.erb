<h1>マイページ</h1>

<h2>登録情報
 <%= link_to "編集する", edit_public_customer_path(@customer.id), class: 'btn btn-sm btn-success' %>
</h2>

<table>
<tbody>
  <tr>
    <td>
      氏名
      <%= @customer.first_name %><%= @customer.last_name %>
    </td>
    <td>
      フリガナ
      <%= @customer.first_name_kana %><%= @customer.last_name_kana %>
    </td>
    <td>
      郵便番号
      <%= @customer.postal_code %>
    </td>
    <td>住所
    <%= @customer.address %></td>
    <td>
      電話番号
      <%= @customer.telephone_number %>
    </td>
    <td>
      メールアドレス
      <%= @customer.email %>
    </td>
  </tr>
</tbody>
</table>

<h2>配送先</h2>
<%= link_to "一覧を見る", public_addresses_path %>

<h2>注文履歴</h2>
<%= link_to "一覧を見る", public_orders_path %>